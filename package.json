{
  "name": "solutionsnplusone-crm-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Unified monorepo for SolutionsNplusOne CRM",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"customer,modules,users,frontend\" -c \"bgBlue,bgGreen,bgMagenta,bgCyan\" \"PORT=4000 npm run start:dev -w ms-customer\" \"PORT=4002 npm run start:dev -w ms-modules\" \"PORT=4001 npm run start:dev -w ms-users\" \"npm run dev -w frontend\"",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
